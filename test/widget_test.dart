// Placeholder test. The default `flutter create` scaffolded a counter-app
// smoke test that referenced `MyApp`, which this project doesn't have.
// Replaced with a trivial sanity check so `flutter test` stays green
// until we have stable UI worth exercising.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('scaffolding placeholder', () {
    expect(1 + 1, equals(2));
  });
}
